services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-kindle_rss}
      POSTGRES_USER: ${POSTGRES_USER:-kindle}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - kindle
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10
    # No host ports: Postgres stays reachable only inside the compose network.

  app:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        GIT_REVISION: ${GIT_REVISION:-unknown}
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: production
      DATABASE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-kindle_rss}
      DATABASE_USER: ${POSTGRES_USER:-kindle}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      APP_PUBLIC_URL: ${APP_PUBLIC_URL:-https://${DOMAIN}}
      ADMIN_EMAILS: ${ADMIN_EMAILS:-}
      MAIL_FROM: ${MAIL_FROM:?set MAIL_FROM}
      SMTP_HOST: ${SMTP_HOST:-smtp.resend.com}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-resend}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_AUTH: ${SMTP_AUTH:-true}
      SMTP_STARTTLS: ${SMTP_STARTTLS:-true}
      REMEMBER_ME_KEY: ${REMEMBER_ME_KEY:?set REMEMBER_ME_KEY}
      FEED_MAX_ENTRIES: ${FEED_MAX_ENTRIES:-100}
      ARTICLE_PAGE_SIZE: ${ARTICLE_PAGE_SIZE:-50}
      MAX_FEEDS_PER_USER: ${MAX_FEEDS_PER_USER:-50}
      MAX_SENDS_PER_DAY: ${MAX_SENDS_PER_DAY:-50}
      DONATE_URL: ${DONATE_URL:-}
    networks:
      - kindle
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/actuator/health | grep -q '\"status\":\"UP\"' || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 50s

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      app:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: ${DOMAIN:?set DOMAIN}
      # Optional: the static marketing/landing page's domain (e.g. extrablatt.app,
      # with DOMAIN set to reader.extrablatt.app). Leave unset to run the app only.
      MARKETING_DOMAIN: ${MARKETING_DOMAIN:-}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ../marketing:/srv/marketing:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - kindle

networks:
  kindle:

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
